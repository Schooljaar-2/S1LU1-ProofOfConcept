<div class="container d-flex flex-column align-items-center justify-content-center" style="min-height: 80vh;">
		<h1 class="display-4 fw-bold text-center mb-2" style="color: #ff3333; letter-spacing: 1px;">Staff Dashboard</h1>
		<div class="dashboard-title-underline mb-5"></div>
		<div class="dashboard-grid">
			<a href="/staff/manageRentals" class="dashboard-btn d-flex flex-column align-items-center justify-content-center">
				<i class="bi bi-plus-square-fill dashboard-icon"></i>
				<span>Create new rental</span>
				<small class="dashboard-desc">Start a new rental for a customer and assign movies.</small>
			</a>
			<a href="/staff/manageStores" class="dashboard-btn d-flex flex-column align-items-center justify-content-center">
				<i class="bi bi-shop-window dashboard-icon"></i>
				<span>View store dashboard</span>
				<small class="dashboard-desc">See store inventory, rental stats, and staff activity.</small>
			</a>
			<a href="/staff/manageMovies" class="dashboard-btn d-flex flex-column align-items-center justify-content-center">
				<i class="bi bi-film dashboard-icon"></i>
				<span>Manage movies</span>
				<small class="dashboard-desc">Add, edit, or remove movies from the catalog.</small>
			</a>
			<a href="/staff/manageCustomers" class="dashboard-btn d-flex flex-column align-items-center justify-content-center">
				<i class="bi bi-people-fill dashboard-icon"></i>
				<span>Manage customers</span>
				<small class="dashboard-desc">View and update customer profiles and rental history.</small>
			</a>
		</div>
		<style>
			/* 2x2 grid for dashboard buttons on desktop */
			.dashboard-grid {
				display: grid;
				grid-template-columns: 1fr;
				gap: 2rem;
				width: 100%;
				max-width: 720px;
				margin: 0 auto;
			}
			@media (min-width: 768px) {
				.dashboard-grid {
					grid-template-columns: 1fr 1fr;
					grid-template-rows: 1fr 1fr;
					gap: 2rem;
				}
			}
		</style>
</div>

	<style>
		.dashboard-btn {
            background: #222;
            color: #fff;
            font-size: 2rem;
            font-weight: 600;
            border-radius: 1rem;
            box-shadow: 0 4px 24px rgba(0,0,0,0.15);
            width: 100%;
            aspect-ratio: 1 / 1;
            min-width: 220px;
            min-height: 220px;
            max-width: 340px;
            max-height: 340px;
            padding: 2rem 1.5rem 1.5rem 1.5rem;
            margin: 1rem auto;
            text-align: center;
            text-decoration: none;
            transition: background 0.2s, color 0.2s, transform 0.2s;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }
		.dashboard-title-underline {
			width: 100%;
			max-width: 420px;
			height: 5px;
			margin: 0 auto 1.5rem auto;
			background: linear-gradient(90deg, #ff3333 0%, #ffc107 100%);
			border-radius: 3px;
		}
		.dashboard-btn:hover, .dashboard-btn:focus {
			background: #ff3333;
			color: #fff;
			transform: translateY(-4px) scale(1.03);
			text-decoration: none;
		}
		.dashboard-icon {
			font-size: 3.5rem;
			margin-bottom: 1rem;
			color: #ff3333;
			transition: color 0.2s;
		}
		.dashboard-btn:hover .dashboard-icon,
		.dashboard-btn:focus .dashboard-icon {
			color: #fff;
		}
		.dashboard-desc {
			display: block;
			font-size: 1.1rem;
			font-weight: 400;
			color: #ccc;
			margin-top: 0.75rem;
		}
	</style>
